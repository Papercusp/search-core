[2026-08-23T08:58:00Z] content-fixer WI-40919: a content-error dispatch's `scope: "superproject"` is relative to the DISPATCHED HARNESS's repo root (here libs/generic/search-core, per `git rev-parse --show-toplevel`), NOT the papercusp monorepo root — probing the file at the monorepo root gives ENOENT and content-lint exit 2, which is the tool refusing to answer, never a clean verdict. Resolve the harness repo root first, then probe with the path relative to it.

{
  "name": "@papercusp/search-core",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "description": "Engine-agnostic search-relevance core: instruction-following rerank steering, live LLM category-match, brand-aware query rewrite, tiered escalation, and the shared eval-harness metric contract. Builds on @papercusp/rerank; no project (Typesense/PG/catalog-schema) deps. Shared across Papercusp and Restart.",
  "license": "UNLICENSED",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "sideEffects": false,
  "scripts": {
    "test": "vitest run --passWithNoTests",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "peerDependencies": {
    "@papercusp/rerank": "*"
  },
  "devDependencies": {
    "vitest": "^4.1.5"
  },
  "publishConfig": {
    "access": "restricted"
  }
}
